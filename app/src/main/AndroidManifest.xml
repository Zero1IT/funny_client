<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.funnynose">

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name="by.funnynose.app.events.CreateEventActivity" android:windowSoftInputMode="adjustResize">
        </activity>
        <activity
            android:name="by.funnynose.app.SplashActivity"
            android:label="funnynose"
            android:screenOrientation="userPortrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="by.funnynose.app.authentication.AuthenticationActivity"
            android:label="Вход"
            android:screenOrientation="userPortrait"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="stateHidden"></activity>
        <activity
            android:name="by.funnynose.app.authentication.RegistrationActivity"
            android:label="Регистрация"
            android:screenOrientation="userPortrait"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="stateHidden"></activity>
        <activity
            android:name="by.funnynose.app.MainActivity"
            android:label="@string/app_name"

            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="userPortrait">
        </activity>

        <activity
            android:name="by.funnynose.app.users.UserActivity"
            android:label="Пользователь"
            android:theme="@style/AppTheme"
            android:screenOrientation="userPortrait">
        </activity>

    </application>

</manifest>